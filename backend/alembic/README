Generic single-database configuration.

sample
docker compose exec backend alembic revision --autogenerate -m "convert datetime columns to timezone-aware"

docker compose exec backend alembic upgrade head